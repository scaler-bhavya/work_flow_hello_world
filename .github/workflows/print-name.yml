name: print-name

on:
  push:
    branches:
      - main

jobs:
  print-name-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print the name
        run: echo "Hello from print-name action!"
